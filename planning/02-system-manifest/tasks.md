# 02 - Manifiesto del sistema

- [x] Crear `desperta.toml` con runtime, Git, politicas y hosts iniciales.
- [x] Separar `repo_path` de `.config/despertaferro`.
- [x] Definir `base_branch` y prefijo de ramas por host.
- [x] Definir allowlist inicial en `config/tracked-paths.txt`.
- [x] Cargar y validar TOML desde el CLI.
- [x] Resolver variables tipo `${HOME}` y `${XDG_STATE_HOME}`.
- [x] Definir perfiles por OS y uso: `base`, `arch`, `macos`, `hyprland`, `work`.
- [ ] Anadir versionado y migraciones del esquema del manifiesto.

